---
layout: layouts/base.njk
---

<section class="page-hero">
  {% if eyebrow %}
  <p class="eyebrow">{{ eyebrow }}</p>
  {% endif %}
  <h1>{{ title }}</h1>
  {% if intro %}
  <p class="lead">{{ intro }}</p>
  {% endif %}
</section>

{{ content | safe }}