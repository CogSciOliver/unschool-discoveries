---
layout: layouts/base.njk
---

<section class="page-hero">
  <h1>{{ title }}</h1>
  {% if intro %}
    <p class="lead">{{ intro }}</p>
  {% endif %}
</section>

<section class="page-body">
  {{ content | safe }}
</section>